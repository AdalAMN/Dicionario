#include "stdio.h"
#include "stdlib.h"
#include "estrutura.h"

int main(){
    int controle = 1
    t_hash* Fabrica = criar_hash(0.7);
    while(controle != 0){
        printf("\n---Menu---\n")
        printf("Selecione 1 para inserir item");
        printf("Selecione 2 para buscar item");
        printf("Selecione 3 para remover item");
        printf("Selecione 4 para atualizar estoque");
        printf("Selecione 0 para fechar");
        controle = int(input())
        switch(controle){
            case(1):

            case(2):

            case(3):

            case(4):

            case(5):
            
        }
    }
}
